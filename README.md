# render_thing
RenderThing is a Vulkan rendering framework aiming to simplify setup of Vulkan projects! I've created it as a way to learn the Vulkan framework and create custom RAII implementations of common Vulkan structures (and other more complex objects!).
